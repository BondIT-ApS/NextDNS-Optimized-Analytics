# NextDNS Optimized Analytics - Environment Configuration Template
# Copy this file to .env and fill in your actual values
# Like having a LEGO instruction manual for your configuration! 🧱

# =============================================================================
# DATABASE CONFIGURATION (Required)
# =============================================================================
# PostgreSQL database connection settings
POSTGRES_USER=nextdns_user
POSTGRES_PASSWORD=secure_password_here
POSTGRES_DB=nextdns
POSTGRES_HOST=db
POSTGRES_PORT=5432

# =============================================================================
# NEXTDNS API CONFIGURATION (Required for log fetching)
# =============================================================================
# Get these from your NextDNS account at https://my.nextdns.io
# Navigate to: Account Settings -> API Keys

# Your NextDNS API key
API_KEY=your_nextdns_api_key_here

# Your NextDNS profile IDs (comma-separated for multiple profiles)
# Example: if your profile URLs are:
#   https://my.nextdns.io/12345/setup (Home)
#   https://my.nextdns.io/67890/setup (Work)
# then your PROFILE_IDS would be: 12345,67890
# Single profile: PROFILE_IDS=12345
# Multiple profiles: PROFILE_IDS=12345,67890,abcdef
PROFILE_IDS=your_profile_ids_here

# =============================================================================
# APPLICATION AUTHENTICATION (Required)
# =============================================================================
# API key for authenticating with the local API
# Generate a secure random string for this
LOCAL_API_KEY=your_secure_local_api_key_here

# =============================================================================
# WEB INTERFACE AUTHENTICATION (Optional)
# =============================================================================
# Enable/disable web interface authentication
# If disabled (false), no login is required to access the web interface
# If enabled (true), users must login with username and password
AUTH_ENABLED=false

# Username for web interface login (default: admin)
# Only used when AUTH_ENABLED=true
AUTH_USERNAME=admin

# Password for web interface login
# You can use a plain password here - it will be handled securely
# Example: AUTH_PASSWORD=LegoRocks
# Only used when AUTH_ENABLED=true
AUTH_PASSWORD=LegoRocks

# Secret key for JWT token signing
# Generate a secure random string (at least 32 characters)
# You can generate one with: openssl rand -hex 32
# Only used when AUTH_ENABLED=true
AUTH_SECRET_KEY=your-secret-key-min-32-chars-change-in-production

# Session timeout in minutes (default: 60)
# How long a user stays logged in before needing to login again
# Only used when AUTH_ENABLED=true
AUTH_SESSION_TIMEOUT=60

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================
# Logging level - controls verbosity of application logs
# DEBUG: Detailed information for debugging (development)
# INFO: General information (production default)
# WARNING: Only warning and error messages
# ERROR: Only error messages
# CRITICAL: Only critical errors
LOG_LEVEL=INFO

# =============================================================================
# DATA FETCHING CONFIGURATION
# =============================================================================
# How often to fetch new DNS logs from NextDNS API (in minutes)
# Default: 60 minutes (1 hour)
# Minimum recommended: 15 minutes
# Maximum recommended: 1440 minutes (24 hours)
FETCH_INTERVAL=60

# Number of DNS log records to fetch per API request
# Default: 100 records per request
# Minimum: 1 record
# Maximum: 1000 records (NextDNS API limit)
# Note: Larger values may increase memory usage and API response time
FETCH_LIMIT=100

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================
# Uncomment and modify these if needed

# Custom PostgreSQL port (default: 5432)
# POSTGRES_PORT=5432

# =============================================================================
# DOCKER SPECIFIC
# =============================================================================
# These are used by Docker Compose and typically don't need to be changed

# Backend API URL (used by frontend)
# BACKEND_API_URL=http://backend:5001/api

# =============================================================================
# QUICK START GUIDE
# =============================================================================
# 1. Copy this file to .env: cp .env.template .env
# 2. Edit .env with your actual values:
#    - Set your NextDNS API_KEY and PROFILE_IDS (comma-separated)
#    - Generate a secure LOCAL_API_KEY
#    - Configure LOG_LEVEL (DEBUG for development, INFO for production)
#    - Set FETCH_INTERVAL (minutes between data fetches)
# 3. Get your NextDNS API key from: https://my.nextdns.io/account
# 4. Find your Profile ID in your NextDNS profile URL
# 5. Run: docker-compose up -d
# 6. Initialize database: cd backend && python manage_db.py init
# 7. Check health: curl http://localhost:5001/health
# 8. View API docs: http://localhost:5001/docs
# 
# 📚 Documentation: Check the docs/ folder for detailed guides
# 🏥 Health monitoring: /health (simple) and /health/detailed (comprehensive)
# 🔍 Interactive API docs: http://localhost:5001/docs
# 
# =============================================================================
# NEW FEATURES IN v2.0
# =============================================================================
# ✨ FastAPI with automatic OpenAPI documentation
# 📊 Enhanced logging with configurable levels and database statistics
# ⏱️  Configurable data fetching intervals via FETCH_INTERVAL
# 🏥 Comprehensive health monitoring with system resource tracking
# 🖥️  Real-time server information (CPU, memory, disk, uptime)
# 🎯 Type-safe API with Pydantic models and validation
# 
# Health Endpoints:
#   GET /health          - Simple health check (status + healthy boolean)
#   GET /health/detailed - Comprehensive system information
# 
# System Monitoring includes:
#   • API and Database health status
#   • Total DNS records count
#   • CPU, memory, and disk utilization
#   • Server platform and architecture info
#   • Current configuration (log level, fetch interval)
